# تعليمات  النشر النهائية - Payment Gateways

## ✅ التغييرات المُنفّذة

1. **حذف جميع البوابات القديمة** (17 بوابة)
2. **الإبقاء على 4 بوابات فقط:**
   - Cash on Delivery (الدفع عند الاستلام)
   - Credit (المحفظة الإلكترونية)
   - InstaPay (مصر)
   - Mada (السعودية)

---

## 🚀 خطوات النشر على السيرفر

```bash
# 1. سحب التحديثات من GitHub
cd /var/www/valdor
git stash  # احفظ أي تعديلات محلية
git pull origin main

# 2. بناء الـ frontend
npm run build

# 3. تشغيل الـ seeder (هيمسح البوابات القديمة ويضيف الجديدة)
php artisan db:seed --class=PaymentGatewayTableSeeder

# 4. مسح الـ cache
php artisan config:clear
php artisan cache:clear
php artisan view:clear
```

---

## 📋 النتيجة المتوقعة

بعد تشغيل الأوامر، في الأدمن بانل → Payment Gateway هتلاقي **4 بوابات فقط**:

1. ✅ **Cash On Delivery** - مُفعّلة
2. ✅ **Credit** - مُفعّلة  
3. ✅ **InstaPay** - مُفعّلة
4. ✅ **Mada** - مُفعّلة

**كل البوابات القديمة اتمسحت نهائياً**

---

## ⚙️ ملاحظات مهمة عن البوابات

### InstaPay

- الحقول الموجودة (`api_key`, `merchant_code`) هي **placeholders**
- InstaPay مش عنده API عام رسمي لحد دلوقتي
- لو هتشتغل مع مزود دفع وسيط (Payment Aggregator) زي:
  - **Fawry**
  - **Paymob**
  - **Accept**
  
  هتدخل بيانات المزود ده في الحقول دي

### Mada  

- Mada بيشتغل من خلال مزودي خدمة زي:
  - **PayTabs**
  - **Moyasar**
  - **Tap Payments**
  - **HyperPay**
  - **Geidea**

- لما تاخد حساب عند واحد منهم، هيدوك:
  - Merchant ID
  - API Key
  - Sandbox/Live URLs

- هتدخل البيانات دي في الأدمن بانل

---

## 🎯 ملخص الكود

**الـ Seeder الجديد بيعمل إيه:**

```php
// 1. بيمسح كل بوابات الدفع القديمة
GatewayOption::where('model_type', PaymentGateway::class)->delete();
PaymentGateway::truncate();

// 2. بيضيف الـ 4 بوابات الجديدة بس
foreach ($this->gateways as $gateway) {
    // إضافة البوابة مع إعداداتها
}
```

**فائدة الطريقة دي:**

- كل مرة تشغل الـ seeder، بيمسح كل حاجة ويضيف الـ 4 بوابات بس
- مفيش فرصة إن بوابات قديمة ترجع تاني
- قاعدة البيانات دايماً نظيفة

---

## ✅ تأكيد النجاح

افتح الأدمن بانل وتأكد:

- عدد البوابات = **4 فقط**
- InstaPay و Mada موجودين ✓
- البوابات القديمة اختفت ✓
