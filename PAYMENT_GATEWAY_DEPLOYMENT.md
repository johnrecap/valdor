# Payment Gateway Deployment Guide

## 📋 Quick Deployment Steps

### على الجهاز المحلي (Local)

```bash
# 1. Commit التغييرات
cd e:\work\valdor\website
git add .
git commit -m "Add InstaPay and Mada payment gateways"
git push origin main
```

---

### على السيرفر (VPS - DigitalOcean)

```bash
# 1. سحب التحديثات
cd /var/www/sitename
git pull origin main

# 2. تشغيل الـ Seeder لإضافة البوابات الجديدة
php artisan db:seed --class=PaymentGatewayTableSeeder

# 3. مسح الـ Cache
php artisan config:clear
php artisan cache:clear
php artisan view:clear
```

---

## ✅ التحقق من التنصيب

### 1. في الأدمن بانل

`https://yourdomain.com/admin/settings/payment-gateway`

**البوابات المُفعّلة:**

- ✅ Cash on Delivery (الدفع عند الاستلام)
- ✅ Credit (المحفظة)
- ✅ **InstaPay** (جديد - مصر)
- ✅ **Mada** (جديد - السعودية)

**البوابات المُعطّلة:**

- ❌ Paypal, Stripe, Flutterwave, وجميع البوابات القديمة

### 2. في الموقع

`https://yourdomain.com/checkout/payment`

ستظهر فقط البوابات الأربع المُفعّلة

---

## ⚙️ إعدادات البوابات الجديدة

### InstaPay (عند الحصول على الحساب)

في الأدمن → Payment Gateway → InstaPay:

- **API Key**: مفتاح API من InstaPay
- **Merchant Code**: كود التاجر
- **Mode**: Sandbox (تجريبي) أو Live (حقيقي)
- **Status**: Enable (تفعيل)

### Mada (عند الحصول على الحساب)

في الأدمن → Payment Gateway → Mada:

- **Merchant ID**: معرّف التاجر
- **API Key**: مفتاح API
- **Mode**: Sandbox (تجريبي) أو Live (حقيقي)
- **Status**: Enable (تفعيل)

---

## 🎯 التغييرات المُنفّذة

✅ إنشاء 2 بوابة دفع جديدة (InstaPay + Mada)  
✅ إضافة شعارات احترافية للبوابتين  
✅ جميع الإعدادات قابلة للتعديل من الأدمن  
✅ تعطيل 17 بوابة قديمة  
✅ لا توجد معلومات سرية في الكود  

---

## ⚠️ ملاحظات مهمة

1. **البوابات الحالية Placeholder**: البوابات شغالة ولكن تحتاج ربط API الحقيقي عند توفر الحسابات
2. **التشغيل مرة واحدة**: لا تشغّل الـ Seeder أكثر من مرة لتجنب التكرار
3. **النسخ الاحتياطي**: احتفظ بنسخة احتياطية من قاعدة البيانات قبل التشغيل

---

## 📞 للدعم

إذا واجهتك أي مشكلة:

1. تأكد من تشغيل MySQL على السيرفر
2. تأكد من صلاحيات الملفات صحيحة
3. تحقق من لوج الأخطاء: `storage/logs/laravel.log`
